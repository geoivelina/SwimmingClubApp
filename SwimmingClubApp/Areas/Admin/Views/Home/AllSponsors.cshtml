@model IEnumerable<SponsorDetailsServiceModel>

@{
    ViewBag.Title = "All Sponsors";
}

<h1 class="text-center">All Sponsors</h1>
<table class="table table-dark">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Name</th>
            <th scope="col">Active</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var sponsor in Model)
        {
            <tr>
                <td scope="col">@sponsor.Id</td>
                <td scope="col">@sponsor.Name</td>
                <td scope="col">@(sponsor.IsActive == true ? "Yes" : "No")</td>
             
                <td scope="col">
                    <a asp-area="" asp-controller="About" asp-action="EditSponsor" asp-route-id="@sponsor.Id" class="btn btn-outline-secondary">Edit</a>
                    <a asp-area="" asp-controller="Abaut" asp-action="DeleteSponsor" asp-route-id="@sponsor.Id" class="btn btn-outline-secondary">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
